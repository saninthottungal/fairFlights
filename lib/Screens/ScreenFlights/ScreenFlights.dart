import 'package:flutter/material.dart';

class ScreenFlights extends StatelessWidget {
  const ScreenFlights({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("screen Flights phewww"),
      ),
    );
  }
}
