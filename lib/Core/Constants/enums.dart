enum TripType {
  oneWay,
  roundTrip,
}

enum ClassType {
  economy,
  premiumEconomy,
  buissness,
  firstClass,
}

enum SelectedField {
  fromField,
  toField,
}

enum TripWay {
  departureWay,
  returnWay,
}
