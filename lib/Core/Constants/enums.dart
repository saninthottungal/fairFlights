enum TripType {
  oneWay,
  roundTrip,
}

enum ClassType {
  economy,
  premiumEconomy,
  buissness,
  firstClass,
}

enum TripDate {
  departureDate,
  returnDate,
}

enum SelectedField {
  fromField,
  toField,
}
