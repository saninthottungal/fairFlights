import 'package:flutter/material.dart';

class ScreenFlight extends StatelessWidget {
  const ScreenFlight({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("screenFlight"),
      ),
    );
  }
}
